# Site configuration

site:
  title: "TimelessInsights"
  tagline: "Curated wisdom for clearer thinking"
  description: "A personal library of timeless ideas from the best thinkers"
  
# Display settings  
display:
  articles_per_page: 20
  recent_count: 10
  
# Build settings
build:
  output_dir: "site/dist"
  search_index: "search-index.json"
